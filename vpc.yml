- name: VPC stack
  amazon.aws.cloudformation:
    stack_name: "{{ stack_name }}"
    state: "{{ stack_state }}"
    region: "{{ aws_region }}"
    template: "{{ template_file }}"
    on_create_failure: DELETE
    # template_parameters:
    #   Hello: world
    tags: "{{ stack_tags }}"
